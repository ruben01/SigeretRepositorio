@model IEnumerable<Lugar>

@{
    ViewBag.Title = "Edificios (Pagina Principal)";
}

<div class="row">
    <div class="col-lg-12">
        <hgroup class="title">
            <h2>Reporte Edificios</h2>
        </hgroup>
        <div class="well">
            <fieldset>
                <legend>Edificios registrados en la aplicación</legend>
                <div class="form-group col-lg-12">
                    <p class="">
                        @Html.ActionLink("Nuevo Edificio", "NuevoEdificio", null, new { @class = "btn btn-primary  fr mb10" })
                    </p>
                </div>
                <div class="form-group scroll form_table col-lg-12 pl0">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Edificio)
                                </th>

                                <th>Opciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Edificio)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Editar", "EditarEdificio", new { id = item.Id }) |
                                        @Html.ActionLink("Eliminar", "EliminarEdificio", new { id = item.Id })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </fieldset>
        </div>
    </div>
</div>
