@{
    ViewBag.Title = "Reporte Solicitudes";
    bool showCustomReport = (ViewBag.IsPost != null) ? true : false;
}


<div class="row">
    <div class="col-lg-12">
        <hgroup class="title">
            <h2>Nuevo Equipo</h2>
        </hgroup>
        <div class="well">
            @using (Html.BeginForm())
            {

                <fieldset class="employe">
                    <legend>Datos del nuevo Equipo a registrar</legend>

                    <div class="row">
                        <div class="form-group col-lg-6">
                            @Html.Label("Fecha Inicio", new { @class = "control-label" })
                            @Html.CTextBox("fechaInicio", null, new { @class = "fecha" })
                        </div>

                        <div class="form-group col-lg-6">
                            @Html.Label("Fecha Fin", new { @class = "control-label" })
                            @Html.CTextBox("fechaFin", null, new { @class = "fecha" })
                        </div>
                    </div>

                    <div class="row form-group col-lg-12">
                        <input type="submit" class="btn btn-default" value="Enviar" />
                        @Html.ActionLink("Regresar", "Index", null, new { @class = "btn btn-default" })
                    </div>
                </fieldset>
            }
        </div>

        @if (!showCustomReport)
        { 
            @Html.Partial("Partial_Solicitudes")
        }
        else 
        {
            @Html.Partial("Partial_Solicitudes", null, ViewData) 
        }
    </div>
</div>


